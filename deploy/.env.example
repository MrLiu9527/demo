# 复制为 .env 放在与 docker-compose.prod.yml 同一目录（勿提交仓库）
# 容器内数据库主机名为 postgres、Redis 为 redis（compose 已覆盖 DATABASE_* / REDIS_URL）

DASHSCOPE_API_KEY=
OPENAI_API_KEY=

# 可选：覆盖默认镜像名（GitHub Actions 部署脚本会写入）
# API_IMAGE=ai-assistant-api:latest
# NGINX_IMAGE=ai-assistant-nginx:latest
