# 子应用独立运行（pnpm dev:chat）时使用
VITE_API_BASE_URL=http://127.0.0.1:8000
# 后端空间与用户 UUID（需与数据库一致；主应用也可在页面输入）
VITE_SPACE_ID=
VITE_USER_ID=
# 可选：默认选中的 agent_id（如 general_assistant）
VITE_DEFAULT_AGENT_ID=

# 主应用（pnpm dev:shell）加载子应用的入口；生产环境为子应用部署 origin + base
VITE_CHAT_ENTRY=//localhost:5174/
