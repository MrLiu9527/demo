# @ai-assistant/chat-app

qiankun 子应用：使用 `@assistant-ui/react`（`useLocalRuntime` + Thread / Composer 原语）对接后端 ` /api/v1/space/{spaceId}/...`。

独立开发：`pnpm dev`（默认端口 5174）。环境变量见仓库 `frontend/.env.example`。
